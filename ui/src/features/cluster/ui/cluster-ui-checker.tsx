import { ReactNode } from 'react'

export function ClusterUiChecker({ children }: { children: ReactNode }) {
  // Simplified for standard wallet adapter - just pass through children
  return children
}
